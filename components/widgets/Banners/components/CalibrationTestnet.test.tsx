/**
 * Testing group for CalibrationTestnet.
 *
 * It includes all the tests related to the operations
 * done in the calibration testnet.
 *
 * @group CalibrationTestnet
 */
describe('CalibrationTestnet', () => {
  /**
   * This test checks if `true` equals `true`.
   * In other words, it's a placeholder test
   * that always passes and needs to be replaced
   * by your own tests.
   *
   * @test {trueShouldBeTrue}
   */
  it('should pass', () => {
    expect(true).toBe(true)
  })
})
